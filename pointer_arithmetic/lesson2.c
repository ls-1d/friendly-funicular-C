#include <stdio.h>
#include <stdlib.h>

void main()
{
    int num;
    printf("size of int: %d\n", sizeof(num));

    int grade = 80;
    printf("size of int: %d\n", sizeof(grade));

    double grade1 = 90.8;

    printf("size of int: %d\n", sizeof(grade1));
    printf("size of int: %d\n", sizeof(double));

}

